<%= form_with(model: own, local: true) do |form| %>
  <% if own.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(own.errors.count, "error") %> prohibited this own from being saved:</h2>

      <ul>
        <% own.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tajir_id %>
    <%= form.number_field :tajir_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
