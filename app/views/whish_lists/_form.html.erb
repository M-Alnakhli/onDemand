<%= form_with(model: whish_list, local: true) do |form| %>
  <% if whish_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(whish_list.errors.count, "error") %> prohibited this whish_list from being saved:</h2>

      <ul>
        <% whish_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
